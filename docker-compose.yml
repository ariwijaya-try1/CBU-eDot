services:
  app:
    build: .
    container_name: eDot_cbu_fastapi
    restart: always
    ports:
      - "127.0.0.1:8001:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload